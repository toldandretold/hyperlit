{
  "version": "v1",
  "note": "LIVING, append-only catalogue of the shapes a conversion fix can take. Seeded from every decision point + structural slot in the pipeline. A post-mortem that finds a NEW shape appends an entry here (status 'discovered', added_in the run date). 'scope' = who acts: model (DeepSeek, rendered into its prompt as an actionable fix-menu) | harness (us, improving the report/prompt/format) | disposition (a verdict, not a fix). 'expressible' = whether the current patch format can apply it: replace (full-function swap) | additive (needs op:add / op:register) | na.",
  "categories": [
    {
      "id": "tune_threshold",
      "name": "Tune a threshold / gate",
      "scope": "model",
      "symptom": "A decision sat near a numeric gate (margin shows 'N vs gate'); a slightly different cutoff would flip it. Low-confidence strategy/linkability/citation forks.",
      "stage": "conversion-core",
      "file": "app/Python/conversion/strategy.py | citations.py | refkeys.py",
      "slot": "a numeric constant inside an existing function (position_ratio 0.65/0.80, fuzzy-year ±3, orphan-share 0.15)",
      "recipe": "op:replace the function holding the constant; change ONLY the threshold; keep the modus operandi (no confident wrong links).",
      "expressible": "replace",
      "risk": "low blast radius, but a threshold tune can regress other documents — re-run the corpus after.",
      "examples": [],
      "status": "seed",
      "added_in": "v1"
    },
    {
      "id": "add_epub_detector",
      "name": "Add an EPUB footnote/structure detector",
      "scope": "model",
      "symptom": "An EPUB's footnotes/markers weren't detected; epub_footnote_detection fired nothing, footnotes_matched is 0 but the source clearly has notes in a shape no detector recognises.",
      "stage": "epub-frontend",
      "file": "app/Python/epub_normalizer.py",
      "slot": "a NEW EpubTransform subclass + register it in the TRANSFORM_PIPELINE list",
      "recipe": "op:add a class subclassing EpubTransform with detect(soup)->bool (inspect only) and transform(soup, log_fn)->None (mutate when detect fired); then op:register the instance into TRANSFORM_PIPELINE in the footnote-detection phase.",
      "expressible": "additive",
      "risk": "low — detectors are composable and independent; a new one only fires when its detect() matches.",
      "examples": [],
      "status": "seed",
      "added_in": "v1"
    },
    {
      "id": "extend_marker_pattern",
      "name": "Extend a marker / citation pattern",
      "scope": "model",
      "symptom": "Markers exist but in a shape the current regex/detector misses ([Author YEAR] square brackets, daggers/asterisks, lettered notes, superscript-in-link).",
      "stage": "conversion-core | frontend",
      "file": "app/Python/conversion/citations.py | footnotes.py | the relevant epub detector",
      "slot": "the recognition regex / branch inside an existing detector or scan function",
      "recipe": "op:replace the function to broaden its pattern; or op:add a sibling scan and call it. Never widen a pattern so far it produces confident-wrong links.",
      "expressible": "replace",
      "risk": "medium — a looser pattern can mis-fire on non-markers; lean on compare_footnote_links.",
      "examples": [],
      "status": "seed",
      "added_in": "v1"
    },
    {
      "id": "add_strategy_fork",
      "name": "Add a new footnote strategy / fork",
      "scope": "model",
      "symptom": "The document's layout matches none of the existing strategies; strategy_selection fell through to a low-confidence default (e.g. footnotes AND citations both massed at the end; per-segment continuous numbering with an offset).",
      "stage": "conversion-core",
      "file": "app/Python/conversion/strategy.py",
      "slot": "a new value in _ALL_STRATEGIES + a branch in analyze_document_structure + a case in _strategy_considered",
      "recipe": "op:register the new name into _ALL_STRATEGIES; op:replace analyze_document_structure to detect + return it; op:replace _strategy_considered to explain why the others lose. Then the linker must handle the new strategy.",
      "expressible": "additive",
      "risk": "high — a new strategy reshapes linking; verify against compare_footnote_links and that existing fixtures stay green.",
      "examples": [],
      "status": "seed",
      "added_in": "v1"
    },
    {
      "id": "add_pipeline_pass",
      "name": "Add a new pipeline pass / stage",
      "scope": "model",
      "symptom": "No existing stage owns the problem — it needs a whole new phase (a [Author YEAR] bracket-citation scan after the paren scan; a cross-segment footnote reconciliation; an offset-correction pass).",
      "stage": "orchestration",
      "file": "app/Python/process_document.py + a new app/Python/conversion/<pass>.py",
      "slot": "a new pure function (extract) + apply step, wired into main()'s ordered passes",
      "recipe": "op:add the new pass function(s); op:replace the orchestrator section that should call it. Record an ASSESSMENT fork for the new decision.",
      "expressible": "additive",
      "risk": "high — touches orchestration; impact_map will run everything. Keep the pass pure + guarded.",
      "examples": [],
      "status": "seed",
      "added_in": "v1"
    },
    {
      "id": "fix_keygen_matching",
      "name": "Fix ref-key generation / matching",
      "scope": "model",
      "symptom": "Citations found but unlinked; evidence.unlinked_sample shows keys that should have matched (et al., multi-author, transliteration, year-suffix collisions, unicode names).",
      "stage": "conversion-core",
      "file": "app/Python/conversion/refkeys.py | citations.py",
      "slot": "generate_ref_keys / the key-match logic",
      "recipe": "op:replace the key generator or matcher to cover the missed shape. Keep the ±-year fuzzy bound conservative — never match across distinct works.",
      "expressible": "replace",
      "risk": "medium — looser matching risks wrong-work links; verify the unlinked_sample now matches its TRUE entry.",
      "examples": [],
      "status": "seed",
      "added_in": "v1"
    },
    {
      "id": "fix_link_seam",
      "name": "Fix the extract→apply linking seam",
      "scope": "model",
      "symptom": "Markers and definitions both exist and correspond, but linking cross-wires / off-by-ones / leaks across section scope (audit shows unmatched on both sides despite matching numbers).",
      "stage": "conversion-core",
      "file": "app/Python/conversion/footnotes.py",
      "slot": "the marker→definition map build or the anchor-apply step",
      "recipe": "op:replace the extract or apply half to correct the pairing. A marker labelled N must carry the id of definition N (per test_linking).",
      "expressible": "replace",
      "risk": "medium — central to correctness; covered by unit/test_linking.py + compare_footnote_links.",
      "examples": [],
      "status": "seed",
      "added_in": "v1"
    },
    {
      "id": "fix_segmentation",
      "name": "Fix segmentation / boundary detection",
      "scope": "model",
      "symptom": "Chapter/segment boundaries mis-detected so per-section numbering merges or splits wrongly — e.g. all definitions pooled while references cover only one segment's range (the aarushi 239–476 / 477 case).",
      "stage": "frontend | orchestration",
      "file": "app/Python/mistral_ocr.py (footnote_meta/segments) | process_document.py",
      "slot": "the segment-boundary detection, or a reconciliation pass that scopes numbering per segment",
      "recipe": "op:replace the boundary detector, or op:add a reconciliation pass that matches refs to defs within the correct segment/offset rather than globally.",
      "expressible": "replace",
      "risk": "high — structural; easy to make it worse. Often pairs with add_pipeline_pass.",
      "examples": [
        "aarushi2025attention"
      ],
      "status": "seed",
      "added_in": "v1"
    },
    {
      "id": "fix_structural_normalization",
      "name": "Fix structural normalization (pre-detection)",
      "scope": "model",
      "symptom": "Malformed upstream HTML blocks detection (calibre spans, nested divs, blockquote-wrapping, dead internal links) so downstream detectors never see clean structure.",
      "stage": "epub-frontend",
      "file": "app/Python/epub_normalizer.py",
      "slot": "a structural EpubTransform (phase 1) — extend an existing one or add a new one",
      "recipe": "op:replace an existing structural transform, or op:add + op:register a new one earlier in TRANSFORM_PIPELINE so footnote detectors then fire.",
      "expressible": "additive",
      "risk": "medium — runs on every EPUB; keep detect() narrow.",
      "examples": [],
      "status": "seed",
      "added_in": "v1"
    },
    {
      "id": "fix_pdf_ocr_assembly",
      "name": "Fix PDF OCR assembly (post-cache)",
      "scope": "model",
      "symptom": "PDF footnotes mis-assembled: page-bottom vs end-of-doc, two-column bleed, running headers leaking, page-footnote renumbering off.",
      "stage": "pdf-frontend",
      "file": "app/Python/mistral_ocr.py",
      "slot": "classify_footnotes / assemble_markdown / renumber_page_footnotes (the ASSEMBLY, replayed from cached OCR)",
      "recipe": "op:replace an assembly function. Do NOT touch fetch_ocr/extract_footer/extract_header — those only affect a fresh OCR and cannot be validated against this cached document.",
      "expressible": "replace",
      "risk": "medium — validated by replaying cached OCR through assembly→html→process_document.",
      "examples": [],
      "status": "seed",
      "added_in": "v1"
    },
    {
      "id": "fix_audit_logic",
      "name": "Fix the audit / verdict logic (a SIGNAL fix)",
      "scope": "model",
      "symptom": "The audit mis-measures — declares 'clean' despite real faults, or miscounts gaps/duplicates/unmatched. Fixing this improves the SIGNAL the loop and humans rely on, not the conversion itself.",
      "stage": "verdict",
      "file": "app/Python/conversion/audit.py | process_document.py (the footnote_audit record)",
      "slot": "compute_footnote_audit or the verdict/decision derivation",
      "recipe": "op:replace the audit function so the verdict reflects all fault classes (a large unmatched-def share is faulty, not clean).",
      "expressible": "replace",
      "risk": "low for conversion output; it changes the assessment goldens, so update them.",
      "examples": [
        "aarushi2025attention"
      ],
      "status": "seed",
      "added_in": "v1"
    },
    {
      "id": "strengthen_suppression_guard",
      "name": "Strengthen a suppression guard (the honest non-link)",
      "scope": "model",
      "symptom": "Links were emitted that might be wrong (numbering can't be trusted) — the right fix is to NOT link, turning a confident-wrong link into an honest missing one.",
      "stage": "conversion-core",
      "file": "app/Python/conversion/strategy.py",
      "slot": "_footnote_numbering_is_linkable (the linkability guard)",
      "recipe": "op:replace the guard to suppress when the numbering correspondence isn't provably safe. A missing link is honest; a confident wrong link is not.",
      "expressible": "replace",
      "risk": "low — strictly safer; may reduce link counts (acceptable per modus operandi).",
      "examples": [],
      "status": "seed",
      "added_in": "v1"
    },
    {
      "id": "fix_sanitization_encoding",
      "name": "Fix sanitization / encoding",
      "scope": "model",
      "symptom": "Mojibake / font-encoding / entity corruption garbles markers or note text so they can't be matched (font_encoding_warning_count > 0).",
      "stage": "conversion-core",
      "file": "app/Python/conversion/sanitize.py",
      "slot": "the sanitize/normalize functions",
      "recipe": "op:replace the sanitizer to repair the specific encoding artefact before detection/linking.",
      "expressible": "replace",
      "risk": "medium — over-aggressive normalization can corrupt good text; scope the repair narrowly.",
      "examples": [],
      "status": "seed",
      "added_in": "v1"
    },
    {
      "id": "enrich_assessment_signal",
      "name": "Enrich the assessment signal (harness)",
      "scope": "harness",
      "symptom": "The pipeline didn't RECORD enough to diagnose the case — a silent branch, missing evidence, or a fork with no 'considered'/would_need. The real defect never surfaced in the flagged forks.",
      "stage": "harness/signal",
      "file": "the responsible conversion module (add ASSESSMENT.record) + flagger in vibe_convert._is_problem",
      "slot": "a new ASSESSMENT.record(...) call, or richer evidence on an existing one",
      "recipe": "WE add the record/evidence so the next run surfaces the defect. Not something DeepSeek can do — it's a report fix.",
      "expressible": "na",
      "risk": "low — diagnostic only, never alters conversion output.",
      "examples": [],
      "status": "seed",
      "added_in": "v1"
    },
    {
      "id": "improve_prompt_or_taxonomy",
      "name": "Improve the prompt / fix-taxonomy (harness)",
      "scope": "harness",
      "symptom": "The signal was present and the fix expressible, but the prompt lacked the vocabulary/permission/recipe for DeepSeek to act — or this very registry was missing/weak on the needed category.",
      "stage": "harness/prompt",
      "file": "app/Python/vibe_convert.py (build_prompt) | tests/conversion/fix_categories.json",
      "slot": "the prompt assembly, or a category entry's recipe/symptom",
      "recipe": "WE sharpen the prompt or the category recipe, then re-run the corpus to measure if DeepSeek's success rate rises.",
      "expressible": "na",
      "risk": "this is the core co-evolution lever; measured by Δscoreboard.",
      "examples": [],
      "status": "seed",
      "added_in": "v1"
    },
    {
      "id": "extend_patch_apply_format",
      "name": "Extend the patch-apply format (harness)",
      "scope": "harness",
      "symptom": "DeepSeek knew the fix but couldn't EXPRESS it — the apply format couldn't represent the edit (a shape beyond replace/add/register).",
      "stage": "harness/format",
      "file": "app/Python/vibe_convert.py (validate_replacements / apply_function_replacements / the op set)",
      "slot": "a new op or a new registry in the allowlist",
      "recipe": "WE add the op/registry under the same safety guards (dangerous-scan + ast re-parse), then expose it in build_prompt.",
      "expressible": "na",
      "risk": "running model code — keep every op behind the existing sandbox + scan + re-parse gates.",
      "examples": [],
      "status": "seed",
      "added_in": "v1"
    },
    {
      "id": "path_b_human",
      "name": "Path-B / human (disposition)",
      "scope": "disposition",
      "symptom": "Genuinely not auto-fixable from what we can give the model — the source is ambiguous, or the fix needs human structural judgement the gate can't verify.",
      "stage": "disposition",
      "file": "na",
      "slot": "na",
      "recipe": "Log it for a human (GitHub issue / path-B). The quality guard correctly rejecting a sloppy fix is the right outcome here, not a failure.",
      "expressible": "na",
      "risk": "the honest floor — don't force a confident-wrong fix to avoid this label.",
      "examples": [],
      "status": "seed",
      "added_in": "v1"
    },
    {
      "id": "fix_book_dir_signal_detection",
      "name": "Fix pathway/source signal detection (harness)",
      "scope": "harness",
      "symptom": "is_epub/is_pdf or pathway is derived from the converted output dir, which lacks the source (original.epub/ocr_response.json), so routing + the prompt's per-pathway caveats are wrong (e.g. a footnote_audit on an EPUB routes to process_document, not epub_normalizer; the EPUB sample/recipe hints never fire).",
      "stage": "harness/signal",
      "file": "app/Python/vibe_convert.py (load_artifacts) | tests/conversion/vibe_eval.py",
      "slot": "carry the detected pathway from the eval/case into load_artifacts instead of sniffing the book_dir, or also look at the case source dir",
      "recipe": "WE thread the pathway/source signal through so modules_for() routes to the real responsible front-end and build_prompt() shows the right caveats + ★ categories.",
      "expressible": "na",
      "risk": "low — diagnostic/routing only; but wrong routing wastes a whole expensive run.",
      "examples": [
        "aarushi2025attention"
      ],
      "status": "discovered"
    }
  ]
}
