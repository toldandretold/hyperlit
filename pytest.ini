[pytest]
# Unit tests for the modular conversion pipeline (app/Python/conversion/).
# The end-to-end fixtures live in tests/conversion/ and run via run_regression.py.
testpaths = tests/conversion/unit
python_files = test_*.py
addopts = -q
